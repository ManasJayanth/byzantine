extends layout

block content
  h1= Byzantine
  form(action='/authentication', method="post")
    | ID
    input(type="text", name="email")
    | password
    input(type="password" name="password")
    input(type="submit")
  if error
    p Error occured